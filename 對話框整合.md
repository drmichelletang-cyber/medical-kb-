# 對話框整合（在 /medical 頁直接跟 Claude 對話、傳附件、寫入知識庫）

讓你在知識庫頁面就地 key-in：問答、上傳 PDF/照片、把新內容加進知識庫——由後台串接 Claude API 與 GitHub。

## 架構一句話
`/medical`（Lovable，登入後）→ Supabase Edge Function `kb-agent`（保管金鑰）→ Claude Opus 4.8（含工具）→ 寫 `.md` 到 GitHub → GitHub Action 重生 HTML → 站上更新。

## 這個 repo 已備好
- `_tools/產生HTML.py`：多輸出 `notes.json`（後台讀它當知識庫 context）。
- `.github/workflows/build.yml`：push `.md` 後自動重生 `index.html` + `notes.json`（你不必再手動跑產生器）。
- `supabase/functions/kb-agent/index.ts`：Edge Function 全文（Messages API + 工具 + GitHub 寫檔 + 附件）。
- `agent/系統提示.md`：後台行為規範（編碼 CLAUDE.md 鐵則）。

## 你要做的（一次性）

### 1. 金鑰
- **Anthropic API key**：platform.anthropic.com 取得。
- **GitHub fine-grained PAT**：限 `drmichelletang-cyber/medical-kb-`、權限 **Contents: Read and write**。

### 2. 部署 Edge Function（擇一）
- 我可經 Supabase MCP 代部署 `kb-agent`；或你用 CLI：`supabase functions deploy kb-agent`。
- 在 Supabase → Edge Functions → **Secrets** 設定：
  ```
  ANTHROPIC_API_KEY = sk-ant-...
  GITHUB_TOKEN      = github_pat_...
  GITHUB_REPO       = drmichelletang-cyber/medical-kb-
  GITHUB_BRANCH     = claude/sweet-hopper-ckn6il
  KB_NOTES_URL      = https://drmichelletang-cyber.github.io/medical-kb-/notes.json
  KB_SYSTEM_URL     = https://raw.githubusercontent.com/drmichelletang-cyber/medical-kb-/claude/sweet-hopper-ckn6il/agent/系統提示.md
  ```
  （`SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` 平台已自動注入。）
- 權限：只有 `user_roles` 內 role=admin 者能用（函式內已檢查）。

### 3. Lovable UI（把下面整段貼給 Lovable）
```
在 /medical 頁（密碼/登入後）加一個「知識庫助手」對話面板，跟現有的知識庫 iframe 並排（桌機左右、手機上下）。

需求：
- 訊息串 + 輸入框 + 「附件」鈕（可選 PDF/圖片，多檔）。送出時呼叫 Supabase Edge Function `kb-agent`，
  body = { action:"chat", messages:[...對話歷史...], attachments:[{media_type, data(base64)}] }，
  帶上使用者的 Supabase JWT 於 Authorization header。
- 回傳的 assistant 文字顯示在訊息串；若回傳 drafts（propose_note/propose_edit），
  每則草稿用卡片顯示「路徑 + 內容預覽」，附「核可並寫入」與「捨棄」兩鈕。
- 按「核可並寫入」時，呼叫同一函式 body = { action:"commit", path, content, message }，成功後顯示 commit 連結並提示「站上稍候自動更新」。
- 配色 深藍 #1E2761 + 金色 #C5993A；只有登入的 admin 看得到此面板。
```

## 分階段
1. **問答（唯讀）**：先只接 `action:"chat"` 的文字問答，驗證管路。
2. **附件/照片**：加附件上傳（base64 帶入 attachments）。
3. **寫入**：開 drafts 卡片 + 核可 → `action:"commit"`。新內容一律 `verified:false` + `#待查核`。

## 端到端驗證
- 問「列出所有待查核項目」→ 應對齊 `notes.json`。
- 上傳一份 PDF 問「摘要重點」→ 回覆引用該檔。
- 「新增一篇證據：…」→ 核可 → repo 出現新 `.md` commit、Action 重生、站上出現帶 `#待查核` 的新筆記。

## 注意
- 金鑰只在 Supabase Edge Function，**永不**進前端或靜態頁。
- 成本：每則訊息耗 Claude API token；面板置於登入後，且後台已對 system+notes 開 prompt cache 降本。
- 靜態 GitHub Pages HTML 仍是唯讀檢視器；對話框只存在於 Lovable `/medical`。
